<common:PageBase
    x:Class="MyShelf.Pages.SearchPage"
    xmlns:common="using:Mendo.UAP.Common"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShelf.Pages"
    xmlns:vm="using:MyShelf.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{x:Bind ViewModel.SearchTerm, Mode=TwoWay}"/>
            <Button Content="Search" Click="{x:Bind ViewModel.SearchClick}"/>
        </StackPanel>
        <GridView ItemsSource="{x:Bind ViewModel.Results, Mode=OneWay}" Grid.Row="1" SelectedItem="{x:Bind ViewModel.SelectedWork, Mode=TwoWay}" SelectionChanged="{x:Bind ViewModel.SelectionChanged}" Grid.ColumnSpan="2">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="vm:WorkViewModel">
                    <TextBlock Text="{x:Bind Title}"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</common:PageBase>
