<Page
    x:Class="MyShelf.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShelf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MyShelf.Controls"
    xmlns:data="using:MyShelf.ViewModels"
    mc:Ignorable="d"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{x:Bind ViewModel.Updates, Mode=OneWay}" SelectionMode="None">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:UpdateViewModel">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Height="50" Width="50">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{x:Bind ImageUrl, Mode=OneWay}" Stretch="None"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock>
                            <Hyperlink><Run Text="{x:Bind UserName, Mode=OneWay}"/></Hyperlink>
                            <Run Text="{x:Bind ActionText, Mode=OneWay}"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ListView ItemsSource="{x:Bind ViewModel.CurrentlyReading, Mode=OneWay}" SelectionMode="None" Grid.Column="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
